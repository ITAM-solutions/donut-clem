# DEPRECATED FILES/SCRIPTS

These scripts and files are not used anymore, but are kept here as an interest
point for developers, to discover past "failures" that lead to the current
state of the model.